// Minimal fixture for the internal/build Railpack integration: enough for
// Railpack's node provider to detect a Node.js app and generate a build
// plan, and, once built, enough to prove the resulting image actually
// runs (see TestClient_BuildRailpack_Live_Node in railpack_test.go).
console.log("levelrail railpack node fixture");
